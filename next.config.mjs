/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'localhost', // Your local development domain
          'supportive-car-b7a635f0ab.media.strapiapp.com' // Add your external image domain
        ],
      },
   
};

export default nextConfig;
